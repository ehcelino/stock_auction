<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1"><%= link_to "Leilão do Galpão", root_path, class:"nav-link" %></span>
    <div class="d-flex">
      <% if user_signed_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to current_user.user_email, user_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to 'Sair', destroy_user_session_path, method: :delete, class:"nav-link" %>
          </li>
        </ul>
      <% else %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Entrar', new_user_session_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Cadastrar', new_user_registration_path, class:"nav-link" %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
